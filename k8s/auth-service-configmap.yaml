apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: default
data:
  # Non-sensitive configuration only
  # Sensitive values moved to auth-service-secrets Secret
  PORT: "8081"
  GO_ENV: "development"
  AUTH_REDIRECT_URI: "https://51467bc2b8ce.ngrok-free.app/auth/callback"
  CORS_ALLOWED_ORIGINS: "http://localhost:3000,https://51467bc2b8ce.ngrok-free.app"
