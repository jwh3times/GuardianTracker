# Root .env file for docker-compose

# Runtime mode is required and accepts exactly development or production.
GO_ENV=development

# Service Ports (host-side; container ports never change). These are offset from
# the canonical 5432/5050/8080 so the stack coexists with other local projects.
# See README.md → Ports.
API_SERVICE_PORT=8081
FRONTEND_PORT=5273
PGADMIN_PORT=5150
PGADMIN_EMAIL=admin@guardiantracker.gg
PGADMIN_PASSWORD=admin
# Throwaway Postgres for the Go test suite (profile: test); sits next to 5532.
TEST_POSTGRES_PORT=5533

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5532
POSTGRES_DB=guardian_tracker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# Bungie API Configuration
BUNGIE_CLIENT_ID=your_bungie_client_id_here
BUNGIE_CLIENT_SECRET=your_bungie_client_secret_here
BUNGIE_API_KEY=your_bungie_api_key_here

# JWT Configuration (keep this secure!)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_minimum_32_characters

# OAuth Configuration — must match the frontend host port (FRONTEND_PORT) AND the
# redirect URI registered in your Bungie application settings.
AUTH_REDIRECT_URI=http://localhost:5273/auth/callback

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5273

# Token encryption (generate with: openssl rand -base64 32). Versions are
# positive SMALLINT identifiers and must never be reused for different keys.
TOKEN_ENCRYPTION_KEY=
TOKEN_ENCRYPTION_KEY_VERSION=1
# Previous encryption key/version (set both only during key rotation)
TOKEN_ENCRYPTION_KEY_PREVIOUS=
TOKEN_ENCRYPTION_KEY_PREVIOUS_VERSION=

# Admin bootstrap — comma-separated Bungie membership IDs pinned to the admin
# role on every login. The only way to mint the first admin; additional admins
# are then granted from the in-app Admin Console. Leave empty for no bootstrap.
# Use your PRIMARY (cross-save) Bungie membership ID — login resolves cross-save
# accounts to the primary membership, so a non-primary platform ID won't match.
ADMIN_MEMBERSHIP_IDS=

# Audit log retention in days (rows + stored client IPs pruned hourly past this)
AUDIT_RETENTION_DAYS=180
# Comma-separated CIDRs/IPs trusted for X-Forwarded-For client-IP resolution
# (set to the platform ingress range in production; empty trusts none)
TRUSTED_PROXIES=

# Bungie OAuth/CDN endpoints — override only for testing against a non-production
# Bungie environment; defaults below match the live Bungie.net endpoints.
# BUNGIE_AUTHORIZE_URL=https://www.bungie.net/en/OAuth/Authorize
# BUNGIE_TOKEN_URL=https://www.bungie.net/platform/app/oauth/token/
# BUNGIE_CDN_BASE_URL=https://www.bungie.net

# Rate limiting for auth endpoints (requests/sec and burst, per client IP)
# AUTH_RATE_LIMIT_RPS=5
# AUTH_RATE_LIMIT_BURST=10

# Maximum accepted request body size in bytes (rejects larger payloads)
# MAX_BODY_BYTES=65536
